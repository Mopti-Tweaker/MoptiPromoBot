services:
  - type: worker
    name: promobot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: PRICEAPI_KEY
        sync: false
